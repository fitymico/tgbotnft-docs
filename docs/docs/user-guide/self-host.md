---
sidebar_position: 6
---

# SELF-HOST: установка на своём сервере

Тариф **SELF-HOST** предназначен для тех, кто хочет запустить фронтенд бота на собственном сервере. Бот подключается к нашему центральному бэкенду — Вам не нужно настраивать API-ключи Telegram вручную.

## Что Вам понадобится

- Сервер (VPS/VDS) с **Linux** (Ubuntu 20.04+ рекомендуется)
- **Docker** и **Docker Compose** (установленные на сервере)
- **Bot Token** от [@BotFather](https://t.me/BotFather)
- **Лицензионный ключ** (получен после оплаты в Service Bot)

## Шаг 1. Получите лицензионный ключ

После оплаты тарифа SELF-HOST в [Service Bot](https://t.me/) нажмите **"Показать лицензионный ключ"**. Ключ имеет формат:

```
SB-XXXXXXXXXXXXXXXX
```

Сохраните его — он понадобится для настройки.

## Шаг 2. Создайте бота в BotFather

Если Вы ещё не создали бота — следуйте инструкции в разделе [Создание бота через BotFather](./create-bot).

## Шаг 3. Подготовьте сервер

Подключитесь к серверу и установите Docker:

```bash
# Обновление пакетов
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com | sh

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
```

> После добавления в группу docker перезайдите в терминал.

## Шаг 4. Клонируйте репозиторий

```bash
git clone https://github.com/pluttan/Telegram-Bot-NFT.git
cd Telegram-Bot-NFT
```

## Шаг 5. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
nano .env
```

Заполните следующие поля:

```env
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_ID=ваш_telegram_id
LICENSE_KEY=ваш_лицензионный_ключ
```

> Ваш **Telegram ID** можно узнать у бота [@userinfobot](https://t.me/userinfobot).

## Шаг 6. Запустите бота

```bash
cd docker
docker compose up -d
```

Проверьте, что контейнер запущен:

```bash
docker ps
```

Вы должны увидеть контейнер `telegram-gift-bot` со статусом `Up`.

## Шаг 7. Проверьте логи

```bash
docker compose logs -f
```

Если бот запустился корректно, в логах появится сообщение об успешном старте.

---

## Полезные команды

| Команда | Описание |
|---------|----------|
| `docker compose up -d` | Запустить бота |
| `docker compose down` | Остановить бота |
| `docker compose restart` | Перезапустить бота |
| `docker compose logs -f` | Просмотр логов в реальном времени |
| `docker compose logs --tail=50` | Последние 50 строк логов |

## Обновление бота

```bash
git pull
docker compose down
docker compose up -d --build
```

### 🔒 Безопасность

> Храните файл `.env` в безопасном месте. **Никогда** не публикуйте его содержимое и не добавляйте в git. Файл `.env` уже добавлен в `.gitignore`.
