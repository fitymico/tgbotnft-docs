# Telegram Gift Bot - Автоматический покупатель подарков NFT

🤖 **Telegram Gift Bot** - это интеллектуальный бот для автоматической покупки подарков (NFT) в Telegram, который работает от вашего имени и помогает выкупать лучшие предложения сразу после их появления.

## ✨ Возможности

- 🎯 **Автоматическое сканирование** подарков в реальном времени
- ⚡ **Мгновенная скупка** при появлении новых подарков
- 🎛️ **Гибкие настройки** распределения звезд по цене подарков
- 🔒 **Безопасная авторизация** через Telegram API
- 📊 **Детальная статистика** и логирование покупок

## � Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/Dimopster/Telegram-Bot-NFT
cd Telegram-Bot-NFT
```

### 2. Настройка переменных окружения

```bash
cp .env.example .env
```

Заполните `.env` файл:

| Переменная | Описание |
|------------|----------|
| `BOT_TOKEN` | Токен бота от [@BotFather](https://t.me/BotFather) |
| `ADMIN_ID` | Ваш Telegram ID (получить у [@userinfobot](https://t.me/userinfobot)) |
| `API_ID` | API ID от [my.telegram.org](https://my.telegram.org) |
| `API_HASH` | API Hash от [my.telegram.org](https://my.telegram.org) |

### 3. Установка зависимостей

```bash
make install
```

### 4. Запуск бота

```bash
make start
```

## 📦 Makefile команды

| Команда | Описание |
|---------|----------|
| `make help` | Показать справку по командам |
| `make install` | Установить все зависимости (Python + Node.js) |
| `make start` | Запустить Telegram бота |
| `make stop` | Остановить все боты |
| `make restart` | Перезапустить боты |
| `make status` | Показать статус ботов |
| `make logs` | Показать последние логи |
| `make clean` | Удалить venv и кэш |
| `make full-clean` | Полная очистка (логи, node_modules, dist) |

### Docker команды

| Команда | Описание |
|---------|----------|
| `make docker-up` | Собрать и запустить в Docker |
| `make docker-down` | Остановить Docker контейнер |
| `make docker-logs` | Показать логи Docker |
| `make docker-restart` | Перезапустить Docker |

## 📁 Структура проекта

```
Telegram-Bot-NFT/
├── Message_Bot/          # Python Telegram бот (aiogram)
│   └── talkbot.py        # Основной файл бота
├── Gift_API/             # TypeScript Gift API
│   └── src/
│       ├── giftBot.ts       # Логика покупки подарков
│       ├── mtprotoClient.ts # MTProto клиент
│       └── processWrap.ts   # Обёртки API
├── scripts/              # Bash скрипты
│   ├── startbot.sh       # Запуск Gift API
│   └── stopbot.sh        # Остановка Gift API
├── data/                 # Данные (gitignored)
│   ├── session.session   # Сессия Telegram
│   ├── status.json       # Статус бота
│   ├── giftID.json       # История подарков
│   └── *.log             # Логи
├── config.py             # Конфигурация Python
├── .env                  # Переменные окружения (gitignored)
├── .env.example          # Пример переменных
├── Makefile              # Команды управления
└── requirements.txt      # Python зависимости
```

## 📖 Подробное руководство

### ⭐ Настройка распределения звезд

>**Сначала осуществляется закупка самых дорогих подарков из списка правил, указанных самим пользователем (если на это хватает звезд на аккаунте пользователя).**
>Это нужно для достижения максимальной выгоды пользователя в закупке подарков.

Бот позволяет гибко настраивать сколько звезд тратить на подарки разной цены:

**Пример формат правил:**
```
<1000 10
>=1000 и <5000 5
>=5000 1
```

**Объяснение:**
- `<1000 10` - осуществлять покупку 10 подарков подарков меньше 1000 звезд (по сути диапазон (0,1000) звезд) при наличии звезд на аккаунте пользователя.
- `>=1000 и <5000 5` - осуществлять покупку 5 подарков в диапазоне цены [1000, 5000) звезд.
- `>=5000 1` - осуществлять покупку 1 подарка стоимостью 5000 звезд.

### Важно:
Покупка подарков в заданных диапазонах происходит **только при наличии доступных подарков** в этих ценовых категориях. 

Например:
- Если в продаже появились подарки стоимостью 1000-4000 звёзд, бот купит 5 подарков как указано в правиле `>=1000 и <5000 5`.
- При этом правило `>=5000 1` **не будет выполнено**, так как подарков стоимостью от 5000 звёзд в продаже нет.

**Операторы сравнения:**
- `<1000` - меньше 1000
- `<=1000` - меньше или равно 1000
- `>1000` - больше 1000
- `>=1000` - больше или равно 1000
- `=1000` - равно 1000
- `>=1000 и <5000` - диапазон от 1000 до 5000

### Управление ботом

#### Основные команды в Telegram:
- **🔧 Настройки** - вход в меню настроек
- **⭐ Распределение звезд** - настройка правил покупки
- **💰 Начать** - запуск сканирования подарков
- **🛑 Остановить** - остановка сканирования
- **📊 Статус** - просмотр текущего статуса
- **📋 Лог-файл** - получение истории покупок

## 🔧 Требования

- Python 3.10+
- Node.js 18+
- npm

## 📝 Лицензия

Проект распространяется под лицензией MIT. Подробнее см. в файле [LICENSE](LICENSE).